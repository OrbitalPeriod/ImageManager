services:
  AnimeTagger:
    ports:
      - "50051:50051"
    profiles: ["Tagger"]

  imagemanager:
    ports:
      - "8080:8080"
    profiles: ["Backend"]

  database:
    ports:
      - "5432:5432"

  frontend:
    profiles: ["Frontend"]
